<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Synergy.WPF.Controls"
                    xmlns:sidebar="clr-namespace:Synergy.WPF.Common.Controls.Sidebar">
    <Style TargetType="{x:Type controls:NormalButton}">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="MinHeight" Value="44"/>
        <Setter Property="MinWidth" Value="44"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NormalButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">

                        <ContentPresenter 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
        <Style.Triggers>
            <Trigger
                Property="IsEnabled"
                Value="False">
                <Setter Property="Foreground" Value="{DynamicResource BtnDisForeground}" />
                <Setter Property="Background" Value="{DynamicResource BtnDisBackground}" />
            </Trigger>

            <Trigger
                Property="IsEnabled"
                Value="True">
                <Setter Property="Foreground" Value="{DynamicResource BtnForeground}" />
                <Setter Property="Background" Value="{DynamicResource BtnBackground}" />
            </Trigger>

            <Trigger 
                Property="IsMouseOver"
                Value="True">
                <Setter Property="Opacity" Value=".7"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Opacity" Value=".4" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Sidebar" TargetType="sidebar:Sidebar">
        <Setter Property="Background" Value="{DynamicResource SidebarBackground}"/>
    </Style>

    <Style x:Key="FrameStyle" TargetType="Frame">
        <Setter Property="Background" Value="{DynamicResource FrameBackground}"/>
    </Style>

    <Style BasedOn="{StaticResource NavItemStyle}" x:Key="ThemedNavItemStyle" TargetType="sidebar:NavItem">
        <Setter Property="IconColor" Value="{DynamicResource SidebarItemColor}"/>
        <Setter Property="HoveredIconColor" Value="{DynamicResource SidebarSlcItemColor}"/>
        <Setter Property="SelectedIconColor" Value="{DynamicResource SidebarSlcItemColor}"/>
    </Style>
</ResourceDictionary>